apiVersion: crd.antrea.io/v1alpha3
kind: ClusterGroup
metadata:
  name: blue-app1
spec:
  podSelector:
    matchLabels:
      app: app1
  namespaceSelector:
    matchLabels:
      kubernetes.io/metadata.name: blue
